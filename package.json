{
  "name": "phenoxis-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-deps": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run preview",
    "dev": "cd frontend && npm run dev",
    "deploy": "npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "react-router-dom": "^6.30.1"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "your-repo-url"
  }
}
